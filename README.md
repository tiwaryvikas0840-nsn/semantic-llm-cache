# semantic-llm-cache
A production-style semantic cache for LLM APIs. Spring Boot + Redis Stack + Gemini. Two-layer design (exact + semantic match), single-flight stampede protection, evaluated with a precision/recall harness.
